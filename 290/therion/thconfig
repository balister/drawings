encoding  utf-8

layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout

#track how the extended is working
log extend

source "290.th"
#source "extend-290.th"

export model -fmt survex -o "290.3d"

#export map -proj plan -layout local -o "290.pdf"
#export map -proj plan -layout local -fmt svg -o "290.svg"

export map -proj extended -layout local -o "290.pdf"
