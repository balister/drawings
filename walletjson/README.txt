Mimic copy of all the wallets contents.json files
so that they are under git version control for safety

All automatic, users should be unaware of this.

The folder structure and filenames are identical to those in
expofiles/surveyscans/
so a complete recovery of corrupted JSON files can be done by a simple copy of the folder tree.